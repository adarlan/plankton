package me.adarlan.dockerflow;

public enum JobStatus {
    DISABLED, WAITING, BLOCKED, SCHEDULED, RUNNING, INTERRUPTED, CANCELED, FAILED, TIMEOUT, FINISHED;

    /*
     * switch (status) { case DISABLED: break; case WAITING: break; case BLOCKED:
     * break; case SCHEDULED: break; case RUNNING: break; case INTERRUPTED: break;
     * case CANCELED: break; case FAILED: break; case TIMEOUT: break; case FINISHED:
     * break; }
     */
}