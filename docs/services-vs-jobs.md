# Plankton - Services or jobs?

The pipeline jobs are under the `services` key
because the Compose Specification defines the components as "services".
But, for CI/CD purposes, it would be better to call it "jobs".
